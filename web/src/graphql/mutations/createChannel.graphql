mutation CreateChannel($groupId: Float!, $name: String!) {
  createChannel(groupId: $groupId, name: $name) {
    channel {
      id
      name
    }
    errors {
      field
      message
    }
  }
}
