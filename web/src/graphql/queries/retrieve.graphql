query RetrieveDM($receiverId: Float!) {
  retrieveDM(receiverId: $receiverId) {
    messages {
      msg
      senderId
      channelId
      createdAt
    }
    errors {
      field
      message
    }
  }
}
